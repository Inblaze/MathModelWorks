 
clear;
clc;
b=0.1;%生育率
ul=0.1;%幼虫死亡率
up=0.1;%蛹死亡率
ua=0.1;%成虫死亡率
L(1)=10;%初始化幼虫数量
P(1)=10;%初始化蛹数量
A(1)=10;%初始化成虫数量
N=100;%迭代代数
for t=1:N%代迭代
    L(t+1)=b*A(t);
    P(t+1)=(1-ul)*L(t+1);
    A(t+1)=(1-up)*P(t)+(1-ua)*A(t);
end
 
plot(1:N+1, L);
hold on;
plot(1:N+1, P);
plot(1:N+1, A);